name: easy_country_picker
description: "A simple flutter package to select country with a textfield which provide country code, country name, currency, phone code and continent."
version: 0.0.1
homepage:

environment:
  sdk: '>=3.2.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

repository: https://github.com/smile675/easy_country_picker.git


flutter:

